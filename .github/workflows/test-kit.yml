name: Test rai-check-kit

on:
  push:
    branches:
      - main
    paths:
      - "packages/rai-check-kit/**"
      - "packages/rai-check-core/**"
      - "packages/rai-check-ml/**"
      - ".github/workflows/test-kit.yml"
  pull_request:
    paths:
      - "packages/rai-check-kit/**"
      - "packages/rai-check-core/**"
      - "packages/rai-check-ml/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          python-version: "3.12"
      - run: uv venv
      - run: uv pip install -e "packages/rai-check-core" -e "packages/rai-check-ml" -e "packages/rai-check-kit" pytest
      - run: uv run pytest packages/rai-check-kit/tests -v
      - run: uv run rai-check --help
