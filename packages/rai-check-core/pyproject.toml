[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rai-check-core"
version = "0.0.1"
description = "Shared audit engine, findings, reports, and CLI for the RAI Check Kit"
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Sai Teja Erukude" }]
requires-python = ">=3.10"
keywords = ["responsible-ai", "audit", "fairness", "trustworthy-ai"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
  "typer>=0.12",
  "rich>=13.0",
  "pyyaml>=6.0",
  "jsonschema>=4.0",
]

[project.scripts]
rai-check = "rai_audit.core.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["src/rai_audit"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""
